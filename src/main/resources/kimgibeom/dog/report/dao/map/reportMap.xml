<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='kimgibeom.dog.report.dao.map.UserMap'>
	<select id='getReports' resultType='report'>
		select title, user_id userId, content, view, reg_date regDate, report_num reportNum
		from kimgibeom.reports
		order by reg_date
	</select>
	
	<select id='getReport' parameterType='int' resultType='report'>
		select title, user_id userId, content, view, reg_date regDate, report_num reportNum
		from kimgibeom.reports
		where report_num = #{reportNum}
	</select>
	
	<insert id='addReport' parameterType='string'>
		insert into kimgibeom.reports (title, user_id, content, view, reg_date, report_num)
		values(#{title}, #{userId}, #{content}, #{view}, sysdate, repost_num_seq.nextval)
	</insert>
	
	<update id='modifyReport' parameterType='report'>
		update kimgibeom.reports
		set title = #{title}, content = #{content}
		where report_num = #{reportNum}
	</update>
	
	<delete id='delReport' parameterType='int'>
		delete kimgibeom.reports
		where report_num = #{reportNum}
	</delete>
</mapper>	