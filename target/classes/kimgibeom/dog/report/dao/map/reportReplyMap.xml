<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='kimgibeom.dog.report.dao.map.ReportReplyMap'>
	<select id='getReportReplies' resultType='ReportReply'>
		select reply.reply_num replyNum, report.report_num reportNum, reply.writer writer, reply.content, reply.reg_date regDate
		from report_replies reply join reports report
		on reply.report_num = report.report_num
		order by reply.reg_date DESC
	</select>
	
	<select id='getReportReply' parameterType='int' resultType='ReportReply'>
		select reply.reply_num replyNum, report.report_num reportNum, reply.writer writer, reply.content, reply.reg_date regDate
		from report_replies reply join reports report
		on reply.report_num = report.report_num	
		where reply_num = #{replyNum}
	</select>
	
	<delete id='delReportReply' parameterType='int'>
		delete report_replies
		where reply_num = #{replyNum}
	</delete>	
</mapper>	